# Dependencies
node_modules/

# TypeScript build artifacts
# (public/js/*.js is compiled output but is committed intentionally so the
# app runs without a build step out of the box — see CLAUDE.md)
*.tsbuildinfo

# OS files
.DS_Store
**/.DS_Store
Thumbs.db

# Editor/IDE
.idea/
*.swp
*.swo

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Env files
.env
.env.local
.env.*.local

# Claude Code local settings (machine-specific, not shared)
.claude/settings.local.json
